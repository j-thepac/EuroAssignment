# rawVisitorDF=rawVisitorDF\
# .withColumn("hits_avg",rawVisitorDF["hits_avg"].cast(IntegerType()))\
# .withColumn("logged_in",rawVisitorDF["logged_in"].cast(BooleanType()))\
# .withColumn("visit_start", udateHandler(rawVisitorDF.visit_start) )\
# .withColumn("visitor_id",regexp_replace(trim(rawVisitorDF.visitor_id),"\.",""))\
# .withColumn("visits",rawVisitorDF.visits.cast(IntegerType()))\

# rawVisitorDF=rawVisitorDF\
# .withColumn("visit_start", to_timestamp(rawVisitorDF.visit_start, "yyyy-MM-dd HH:mm:ss"))\
# .withColumn("visitor_id", when(trim(rawVisitorDF.visitor_id) == "null",lit(0) ).otherwise(rawVisitorDF.visitor_id))

# rawVisitorDF=rawVisitorDF\
# .withColumn("date", date_format(rawVisitorDF.visit_start,"yyyy-MM-dd").cast(DateType()))\
# .na.fill("0",["visitor_id"])

# rawVisitorDF=rawVisitorDF.withColumn("visitor_id",rawVisitorDF.visitor_id.cast( DecimalType(38, 0) ))